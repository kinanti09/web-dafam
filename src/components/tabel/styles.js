const styles = theme => ({
    root: {
      width: '100%',
      marginTop: theme.spacing(3),
      overflowX: 'auto',
    },
    table: {
      maxWidth: '100%',
      border: '1px solid ',
      paddingLeft: 100,
      paddingRight: 100,
    },
    textField: {
      marginLeft: theme.spacing(1),
      marginRight: theme.spacing(1),
    },
});

export default styles;